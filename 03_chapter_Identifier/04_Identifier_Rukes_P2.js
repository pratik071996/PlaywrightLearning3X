var name="Test";

var firstName= "Test";
var This_is_a_very_long_variablename= "Test";
var lastName="User";


// Naming conventions for identifiers

// 1. camelCase (standard for JS variables and functions)

let userName= "camelCase";
let totalPrice = 99.099;
let isLoggedIn = true;

// 2. PascalCase (standard for JS classes and constructors)

let UserProfile = "PascalCase";
let ShoppingCart = "Class Name Style";

//3. snake_case (underscore separated words)

let user_name = "snake_case";
let total_price = 99.099;
let is_logged_in = true;

// 4. SCREAMING_SNAKE_CASE (all uppercase letters with underscores, USED FOR CONSTANTS)

const MAX_SIZE = 100;
    const API_KEY = "abc123";
    const DATBASE_URL = "localhost";

    //5. Hungarian Notation (prefixes to indicate the type of variable, prefix with type - olderstyle)

    let strUserName = "Hungarian Notation";
    let intTotalPrice = 99.099;
    let boolIsLoggedIn = true;
    let bActive = true; //boolean
    let nCount = 10; //number
    let arrItems = [1, 2, 3]; //array